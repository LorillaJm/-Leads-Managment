{
  "framework": "vite",
  "buildCommand": "npm install && cd packages/shared && npm run build && cd ../../apps/web && npm run build && pwd && ls -la && cd ../.. && pwd && ls -la apps/web/ && cp -r apps/web/dist ./dist",
  "outputDirectory": "dist",
  "installCommand": "npm install"
}
