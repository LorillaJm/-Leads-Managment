{
  "version": 2,
  "buildCommand": "npm install && npm run build --workspace=@lead-management/api && npm run db:generate --workspace=@lead-management/api",
  "installCommand": "npm install",
  "routes": [
    {
      "src": "/(.*)",
      "dest": "/api/index.js"
    }
  ]
}
