{
  "framework": "vite",
  "buildCommand": "npm install && cd packages/shared && npm run build && cd ../../apps/web && npm run build && mv dist ../../dist",
  "outputDirectory": "dist",
  "installCommand": "npm install"
}
