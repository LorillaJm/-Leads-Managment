{
  "buildCommand": "npm install && npm run build --workspace=@lead-management/shared && npm run build --workspace=@lead-management/web",
  "outputDirectory": "apps/web/dist",
  "installCommand": "npm install"
}
